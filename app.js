let mag=[],size=[];const rd=(f,cb)=>{const r=new FileReader();r.onload=e=>{const wb=XLSX.read(e.target.result,{type:'array'});cb(XLSX.utils.sheet_to_json(wb.Sheets[wb.SheetNames[0]],{defval:''}))};r.readAsArrayBuffer(f)};mag.onchange=e=>rd(e.target.files[0],d=>{mag=d;upd()});size.onchange=e=>rd(e.target.files[0],d=>{size=d;upd()});function upd(){stato.innerHTML=`Magazzino: <b>${mag.length}</b> righe · SIZE: <b>${size.length}</b> righe`;if(mag.length&&size.length)merge()}function merge(){const m=new Map();size.forEach(r=>m.set(String(r.PRODOTTO||r.Prodotto||'').trim().toUpperCase(),r));const rows=mag.map(r=>{const n=String(r.PRODOTTO||r.Prodotto||r.DESCRIZIONE||'').trim();const s=m.get(n.toUpperCase())||{};const ini=+r['Conteggio iniziale']||+r.Iniziale||+r.Giacenza||0;const dan=+r.Danni||0;const ven=+r.Venduto||0;const att=ini-dan-ven;return{name:n,ini,box:s.BOX||'',sl:s.SLEEVE||'',sg:s['C.B. SINGOLO P.']||'',att}});draw(rows);window.rows=rows}function draw(rows){body.innerHTML='';rows.forEach(r=>{const tr=document.createElement('tr');tr.innerHTML=`<td>${r.name}</td><td>${r.ini}</td><td>${r.box}</td><td>${r.sl}</td><td>${r.sg}</td><td>${r.att}</td><td><input class=qty type=number value="${r.att}"></td><td>0</td>`;const i=tr.querySelector('input');const d=tr.lastElementChild;i.oninput=()=>d.textContent=(+i.value||0)-r.att;body.appendChild(tr)})}search.oninput=e=>{const q=e.target.value.toLowerCase();draw((window.rows||[]).filter(r=>r.name.toLowerCase().includes(q)))};